<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="MyDialog" parent="Theme.AppCompat.Dialog.Alert">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/DialogAnim</item>
    </style>

    <!--
    ThemeOverlay.MaterialComponents.BottomSheetDialog - fit screen
    Theme.AppCompat.Light - Background transparent
    -->
    <style name="MyBottomDialog" parent="Theme.AppCompat.Light">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/DialogAnim</item>
    </style>
</resources>